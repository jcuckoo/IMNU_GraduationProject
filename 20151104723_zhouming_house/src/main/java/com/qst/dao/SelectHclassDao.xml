<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.qst.dao.SelectHclassDao">

		<select id="getHclassByBean"
		parameterType="Hclass"
		resultType="Hclass">
		
		<!-- select class_id as class_id,
		class_name as class_name
		
		from h_hclass
		where  1=1  -->
		select * from h_hclass
		</select>
	
</mapper>