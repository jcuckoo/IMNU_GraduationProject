<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.qst.dao.SelectUserDao">

		<select id="getUserByBean"
		parameterType="User"
		resultType="User">
		select * from h_user
		
		</select>
		
		<select id="getSelectUserByBean"
		parameterType="User"
		resultType="User">
		
		select user_id as user_id,
		user_name as user_name,
		user_phone as user_phone,
		user_passwd as user_passwd
		from h_user
		where  1=1 
		<if test=" user_name!=null and user_name!='' ">
			and user_name  = #{user_name}
			</if>
			
		<!-- select * from h_user where user_name = #{user_name} -->
		</select>
	
</mapper>