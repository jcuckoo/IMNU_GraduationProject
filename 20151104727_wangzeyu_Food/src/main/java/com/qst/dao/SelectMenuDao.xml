<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.qst.dao.SelectMenuDao">

		<select id="getMenuByBean"
		parameterType="Menu"
		resultType="Menu">
		
		select menu_id as menu_id,
		menu_name as menu_name,
		menu_price as menu_price,
		menuclass_id as menuclass_id
		from elem_menu
		where  1=1 
		</select>
		
		
		<select id="getSelectMenuByBean"
		parameterType="Menu"
		resultType="Menu">
		
		select menu_id as menu_id,
		menu_name as menu_name,
		menu_price as menu_price,
		menuclass_id as menuclass_id
		from elem_menu
		where  1=1 
		<if test=" menu_name!=null and menu_name!='' ">
			and menu_name  = #{menu_name}
			</if>
		</select>
	
</mapper>