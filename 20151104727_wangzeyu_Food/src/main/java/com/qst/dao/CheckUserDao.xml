<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.qst.dao.CheckUserDao">

		<select id="getUserByBean"
		parameterType="User"
		resultType="User">
		
		select n_userid as n_userid,
		n_username as n_username,
		n_address as n_address,
		n_tell as n_tell,
		from food
		where  1=1 
		</select>
		
		<select id="getCheckUserByBean"
		parameterType="User"
		resultType="User">
		
		select n_userid as n_userid,
		n_username as n_username,
		n_address as n_address,
		n_tell as n_tell,
		from food
		where  1=1 
		<if test=" n_username!=null and n_username!='' ">
			and n_username  = #{n_username}
			</if>
		</select>
	
</mapper>