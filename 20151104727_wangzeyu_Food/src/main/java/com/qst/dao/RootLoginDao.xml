<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.qst.dao.RootLoginDao">

		<select id="getRootLoginByBean"
		parameterType="Root"
		resultType="Root">
		
		select id as id,
		name as name,
		pasw as pasw
		from elem_root
		where  1=1 
		<if test=" name!=null and name!='' ">
			and name  = #{name}
			</if>
		</select>
	
</mapper>