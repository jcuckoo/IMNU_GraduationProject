<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.qst.dao.SelectMenuClassDao">

		<select id="getMenuClassByBean"
		parameterType="MenuClass"
		resultType="MenuClass">
		
		select menuclass_id as menuclass_id,
		menuclass_name as menuclass_name
		
		from elem_menuclass
		where  1=1 
		</select>
	
</mapper>